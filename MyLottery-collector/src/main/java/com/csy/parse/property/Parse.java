package com.csy.parse.property;


public class Parse {
	public static void jsk3ParseProperty(String sNum) {

		for (int i = 0; i < sNum.length(); i++) {
			String num = sNum.substring(i, i);
			int parseInt = Integer.parseInt(num);
			if (parseInt >= 4 && parseInt <= 10) {

			}
		}
	}

}
